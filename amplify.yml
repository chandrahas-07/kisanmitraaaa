version: 1
frontend:
  phases:
    preBuild:
      commands:
        - rm -rf node_modules
        - npm install --legacy-peer-deps
    build:
      commands:
        - npm run build --legacy-peer-deps
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - .npm
